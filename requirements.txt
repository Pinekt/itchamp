fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
# [asyncio] обязателен: тянет greenlet, без него async-движок SQLAlchemy падает.
# На Linux/x86_64 greenlet ставился сам, на macOS (arm64) — нет, и сервер не стартовал.
SQLAlchemy[asyncio]==2.0.35
aiosqlite==0.20.0
asyncpg==0.29.0

# --- информационная безопасность (все лицензии разрешительные, патентно чисто) ---
argon2-cffi==25.1.0     # Argon2id — хеширование паролей, лицензия MIT
PyJWT==2.13.0           # токен сеанса HS256, лицензия MIT
python-multipart==0.0.32  # разбор форм, лицензия Apache-2.0
