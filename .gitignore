.venv/
__pycache__/
.pytest_cache/
# секреты стенда (ключ подписи токенов и т. п.) — в git не попадают
.env
*.pyc
.DS_Store
*.db
*.db-journal
*.db-wal
ktk.db

# Материалы организаторов — интеллектуальная собственность Партнёров
# IT-чемпионата. НЕ публиковать в репозитории (см. последний слайд кейса).
# Лежат локально у каждого, скачиваются из ПО «CASE-IN Симулятор».
materials/
