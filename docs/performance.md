# Время отклика и задержки КТК ЭЛОУ-АВТ

Снято скриптом `scripts/measure_latency.py`, значения — миллисекунды.
Файл перевыпускается командой:

```bash
python scripts/measure_latency.py --out docs/performance.md
```

## Условия замера

- **Дата** — 2026-07-28 05:48:47
- **Платформа** — Linux 6.18.5, x86_64
- **Python** — 3.11.15
- **Процессор** — 4 логических ядер
- **База данных** — SQLite (временный файл)
- **Сеть** — клиент и сервер на одной машине, сетевая задержка не входит в замер
- **Такт симуляции** — 1000 мс
- **Разброс** — замеры сняты на машине, где выполняются и другие задачи; от прогона к прогону p95 отклика гуляет в разы, поэтому сравнивать имеет смысл с нормативом, а не два прогона между собой

## Результаты

| Показатель | Замеров | Среднее | p50 | p95 | p99 | Макс | Норматив p95 | Примечание |
|---|---:|---:|---:|---:|---:|---:|---|---|
| Вход в систему (Argon2id) | 20 | 64.1 | 63.4 | 69.5 | 74.8 | 76.1 | ≤ 500 · уложились | один раз за смену |
| GET /api/me | 200 | 3.9 | 3.9 | 4.5 | 5.0 | 5.2 | ≤ 300 · уложились | проверка сеанса, каждая загрузка страницы |
| GET /api/scenarios | 200 | 6.1 | 5.9 | 7.1 | 8.6 | 12.2 | ≤ 300 · уложились | список сценариев |
| GET /api/sessions | 200 | 6.7 | 6.3 | 7.7 | 13.0 | 29.2 | ≤ 300 · уложились | список тренировок |
| Отклик на команду оператора | 200 | 7.0 | 6.1 | 9.5 | 27.2 | 30.9 | ≤ 200 · уложились | на идущей тренировке |
| Период выдачи телеметрии | 30 | 1000.0 | 1000.0 | 1001.6 | 1002.6 | 1003.0 | ≤ 1050 · уложились | интервал между снимками состояния |
| GET /api/sessions/{id}/debrief | 200 | 22.3 | 21.7 | 25.1 | 27.6 | 58.4 | ≤ 300 · уложились | разбор целиком, точек телеметрии: 31 |
| GET /api/sessions/{id}/debrief (длинная) | 50 | 70.2 | 61.7 | 99.5 | 106.0 | 109.6 | ≤ 300 · уложились | тренировка ~30 мин, точек телеметрии: 1800 |
| Шаг матмодели (без сети и БД) | 20000 | 0.009 | 0.008 | 0.015 | 0.024 | 0.060 | ≤ 50 · уложились | запас цифрового двойника |

## Выводы

- **Отклик на команду оператора** — p50 6 мс, p95 9 мс при нормативе 200 мс. Норматив выдержан. Худший случай — 31 мс: это такты, на которых команда попадает в момент рассылки телеметрии и записи её в БД, потому что запись идёт в том же цикле событий.
- **Период выдачи телеметрии** — p95 1001.6 мс при такте 1000 мс, отклонение +1.6 мс. Такт отсчитывается от расписания, а не от конца обработки, поэтому задержка не накапливается: за получасовую тренировку модельное время не расходится с реальным.
- **Разбор тренировки** открывается за 100 мс (p95) на получасовой тренировке — это один запрос, отдающий сразу журнал, телеметрию, ошибки и оценку.
- **Вход в систему** — 69 мс (p95). Это цена Argon2id: стойкость к перебору покупается временем счёта. На отклик рабочего места не влияет — вход выполняется один раз за смену.
- **Запас цифрового двойника** — шаг матмодели 0.015 мс, это примерно в 69 000 раз быстрее такта. Замена заглушек на реальные модели ректификации и теплообмена упирается не в такт симуляции.

**Итог:** нормативы выдержаны по всем показателям.
